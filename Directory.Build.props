<Project>
  <PropertyGroup>
    <!-- Pin restores to committed packages.lock.json files for reproducible builds. -->
    <RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>
  </PropertyGroup>
</Project>
